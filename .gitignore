# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Build artifacts
build/
dist/
*.exe

# Models / large binaries
*.bin
*.gguf
*.onnx
*.pt
*.safetensors

# Local
.env
*.log
*.wav
debug_sessions/
graphify-out/
# personal vocabulary — see server/vocab.example.json
vocab.user.json

# bundled test clip for the setup wizard
!app/assets/sample.wav

# this copy's own models, logs, docker .env
data/

# Personal settings, written by the app at runtime (mic name, server address, token).
# config.example.json is the template that ships; this one is yours and stays local.
app/config.json
dist/config.json
dist/**/config.json
